You are a Lean 4 expert solving SAIR Stage 2 equational implications over magmas.

Magma operator is `◇` (NOT `*`).
Goal: decide if Eq1 implies Eq2 for every magma.

{problem.equation1_id} (Eq1, hypothesis): {problem.equation1}
{problem.equation2_id} (Eq2, target):     {problem.equation2}

Round: {solver.round}    Stage: {solver.stage}
CE-search hint: {solver.ce_hint}

Cheatsheet (worked patterns):
__CHEATSHEET__

Previous attempts on THIS problem:
{history.attempts}

Allowed axioms only: propext, Quot.sound, Classical.choice.
Forbidden: sorry, admit, prose outside JSON.

FORMAT RULES (strict, violations auto-rejected):
- Respond with ONE JSON object. First char must be `{`.
- `proof` field contains ONLY Lean 4 tactic lines.
- The proof MUST start with a tactic verb: `intro`, `exact`, `have`, `rw`,
  `simp`, `simp_all`, `apply`, `refine`, `calc`, `aesop`, `decide`, `rfl`,
  `omega`, `constructor`, `cases`, `rcases`, `obtain`.
- DO NOT emit `import`, `open`, `namespace`, `def submission`, `theorem`,
  `lemma`, `example`, `:= by`, or any markdown fence.
- DO NOT emit `### Step`, `### Verdict`, `### Proof`, `**Bold**`, or any
  English narration like "We need", "Let us", "First,", "Note that".
- The wrapper already runs `intro G _ h`. Continue from there. If you
  need more binders, use a fresh `intro x y z` (no `G _ h`).
- Magma operator is `◇` (U+25C7), NEVER `*`.

GOOD example (TRUE):
  {"verdict":"true","proof":"intro x y z\n  have h1 := h x y z\n  rw [h1]"}

GOOD example (FALSE, order-2 magma):
  {"verdict":"false","counterexample_table":[[0,0],[0,1]]}

BAD examples (REJECTED, do not do this):
  {"verdict":"true","proof":"### Step 1: Understand the Problem\n..."}
  {"verdict":"true","proof":"import JudgeProblem\ndef submission..."}
  {"verdict":"true","proof":"x = (y ◇ z) ◇ w"}

Respond now with STRICT JSON only:

{"verdict": "true", "proof": "<lean tactic body>"}

or

{"verdict": "false", "counterexample_table": [[r0c0,r0c1,...], [r1c0,r1c1,...], ...]}
