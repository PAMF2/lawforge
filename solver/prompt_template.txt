You are solving equational-theory implication problems in Lean 4 over
magmas with binary operator `◇` (Unicode diamond, U+25C7).

Eq1 (hypothesis): {eq1}
Eq2 (goal):       {eq2}

If Eq1 implies Eq2 universally, emit ONLY the Lean tactic body that closes
the goal `EquationRHS G` given `h : EquationLHS G`. The harness wraps your
body with:

  import JudgeProblem
  def submission : Goal := by
    intro G _ h
    <YOUR BODY HERE>

Constraints:
- Tactic body only. No `import`, no `theorem`, no `def submission`.
- Use `◇` as the magma operator, NOT `*`.
- Allowed axioms: propext, Quot.sound, Classical.choice.
- Forbidden: `sorry`, `admit`.
- Common tactics: `intro x y z`, `rw [h]`, `exact h _ _`, `simp [h]`, `aesop`.

Patterns from accepted proofs:
{cheatsheet}

Counterexample search hint: {ce_hint}
