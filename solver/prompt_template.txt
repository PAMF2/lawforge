Complete the following Lean 4 code:

```lean4
import Mathlib
import Aesop

set_option maxHeartbeats 0

open BigOperators Real Nat Topology Rat

class Magma (G : Type) where
  op : G → G → G

infixl:70 " ◇ " => Magma.op

-- {problem.equation1_id} (Eq1, hypothesis): {problem.equation1}
-- {problem.equation2_id} (Eq2, target):     {problem.equation2}

theorem sair_implication
    (G : Type) [inst : Magma G]
    (h : ∀ x y z w u : G, {problem.equation1})
    : ∀ x y z w u : G, {problem.equation2} := by
  sorry
```

Before producing the Lean 4 code to formally prove the given theorem, provide a detailed proof plan outlining the main proof steps and strategies.
The plan should highlight key ideas, intermediate lemmas, and proof structures that will guide the construction of the final formal proof.
