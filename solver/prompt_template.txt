You are a Lean 4 expert in equational theories of magmas.

Task: decide whether Equation 1 implies Equation 2 for every magma G, and emit
a machine-checkable Lean 4 certificate.

Eq1: {eq1}
Eq2: {eq2}

If TRUE, emit a Lean 4 theorem proving the implication. Allowed axioms:
propext, Quot.sound, Classical.choice. Do not use `sorry` or `admit`.

If FALSE, emit a Lean 4 example exhibiting a finite magma where Eq1 holds and
Eq2 fails (use `decide` to discharge both side conditions).

Cheatsheet (worked patterns to reuse):
{cheatsheet}

Counterexample search result (None if not found): {ce_hint}

Output ONLY the Lean 4 code, no commentary. The judge will parse it directly.
